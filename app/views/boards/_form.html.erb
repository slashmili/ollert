<div  id="new-board" class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title">Add Board</h3>
  </div>

  <div class="panel-body">
    <%= form_for(@new_board) do |f| %>
      <% if @new_board.errors.any? %>
      <div id="error_explanation">
      <h2><%= pluralize(@new_board.errors.count, "error") %> prohibited this board from being saved:</h2>

      <ul>
        <% @new_board.errors.full_messages.each do |message| %>
          <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :title %><br>
      <%= f.text_field :title, class: "form-control" %>
    </div>
    <div class="actions">
      <%= f.submit class: 'btn btn-success', id: 'board-submit' %>
    </div>
  <% end %>
  </div>
</div>
